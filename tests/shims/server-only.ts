// Vitest stub for `server-only`. The real package throws when imported
// outside a Next.js server context. In tests we treat it as a no-op so
// server-only modules can be imported freely.
export {};
